---
import { leetCodeData } from "../data/leetcode";
import { Trophy, Calendar, Users, TrendingUp } from "lucide-react";

const { contests } = leetCodeData;
---

<div class="neo-card p-6">
	<h3 class="text-lg font-bold mb-4">Recent Contest Performance</h3>
	{
		contests.length > 0 ? (
			<div class="space-y-3">
				{contests.slice(0, 5).map((contest) => (
					<div
						class="neo-card p-4 flex flex-col gap-3"
						style="box-shadow:var(--shadow-2xs);"
					>
						<div class="flex items-center justify-between">
							<h4 class="font-semibold">{contest.title}</h4>
							<div class="flex items-center gap-3 text-xs text-muted-foreground">
								<span>
									{new Date(contest.startTime * 1000).toLocaleDateString()}
								</span>
								<span>Rank #{contest.rank.toLocaleString()}</span>
								<span>
									{contest.ratingChange > 0 ? "+" : ""}
									{contest.ratingChange}
								</span>
							</div>
						</div>
						<div class="flex items-center justify-between text-xs">
							<p class="text-muted-foreground">
								Solved {contest.problemsSolved}/{contest.participants}
							</p>
							<p class="font-bold">{contest.score} pts</p>
						</div>
					</div>
				))}
			</div>
		) : (
			<div class="text-center py-6">
				<div class="neo-card inline-flex p-3 mb-4">
					<Trophy className="w-6 h-6" />
				</div>
				<p class="text-sm text-muted-foreground">No contest data available</p>
			</div>
		)
	}
</div>
